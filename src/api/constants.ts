export const WALK_SPEED = 4.3 / 20; // meters per tick
export const RUN_SPEED = 5.612 / 20; // meters per tick
export const DRAG = 0.98; // y velocity multiplicator per tick when not on ground
export const GRAVITY = 0.08; // m/t²; subtracted from y velocity per tick when not on ground
export const JUMP_FORCE = 0.42; // m/t; applied to velocity when starting a jump

export const PLAYER_APOTHEM = 0.3;
export const PLAYER_HEIGHT_STANDING = 1.8;
